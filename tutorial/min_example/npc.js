// user.script -> message
// user.script{} -> different message

function(c, args){
    var l = #s.scripts.lib();
    // quasi-code
    var msg1 = "hello world"
    var msg2 = "continue dialog"
    var actual_msg = ""
    for(var i in args){
      i = 0;
      if (l.is_def(args[i]==True)} { i+=1 };
      if (i>0){actual_msg=msg2}else{actual_msg=msg1
      }
    return actual_msg
  }
